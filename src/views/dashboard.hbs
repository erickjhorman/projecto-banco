<div class="container p-5">
  <div class="row">
    {{#if admin}}
    <h2>Eres usuario ADMIN</h2>
    {{/if}}
    <div class="col-md-5">
      {{> accounts/accountForm }}
    </div>
    {{#if admin}}
    <div class="col-md-7">
      {{> accounts/accountTable }}
    </div>
    {{/if}}
     <div>
  </div>
</div>