

{{#if users}}
  <table class="table table-bordered table-hover table-white">
    <thead>
      <tr>
        <th>N°</th>
        <!--<th>Title</th>-->
        <th>id</th>
        <th>username</th>
         <th>email</th>
         <th>Options</th>
      </tr>
    </thead>
    <tbody>
      {{#each users}}
        <tr>
          <td>{{@index}}</td>
           <!--<td>{{title}}</td>-->
          <td>{{_id}}</td>
          <td >{{username}}</td>
          <td>{{email}}</td>
            <td>
            <a href="/accounts/{{_id}}" class="btn btn-danger">
              Agregar usuario
            </a>
              <a href="/users/{{_id}}/delete" class="btn btn-danger">
              Eliminar
            </a>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
{{else}}
  <div class="h-100 d-flex align-items-center justify-content-center">
    <div class="text-center text-white">
      <h1>No users</h1>
      <img src="/icons/empty.svg" class="w-75" />
    </div>

  </div>
{{/if}}